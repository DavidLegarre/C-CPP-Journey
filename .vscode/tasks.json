{
    "tasks": [
        {
            "label": "Build Sub-Project",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${fileDirname}" // Runs 'make' inside the snake folder
            },
            "group": "build",
            "problemMatcher": ["$gcc"]
        }
    ]
}